# Espace tests logique

Les scripts de tests executables sont maintenus dans:

- `../scripts/tests/`

Ce dossier documente l architecture cible et facilite la navigation du depot.

Pour executer toute la suite:

```bash
./scripts/tests/lancer_suite.sh
```
